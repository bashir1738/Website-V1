# Base URL of the Blockfuse Labs backend API (see API.pdf for the full reference).
# Defaults to the deployed production API if unset — override for local backend dev.
NEXT_PUBLIC_API_URL=https://website-v1-vrl5.onrender.com/api
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
